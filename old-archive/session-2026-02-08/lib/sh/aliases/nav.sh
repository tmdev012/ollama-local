#!/bin/bash
# NAVIGATION & CROSS-REPO ALIASES | Group: nav
alias cds='cd ${SASHI_HOME:-$HOME/ollama-local}'
alias cdp='cd $HOME/persist-memory-probe'
alias cdk='cd $HOME/kanban-pmo'
alias cdc='cd $HOME/.claude'
alias termux-sync='${SASHI_HOME:-$HOME/ollama-local}/scripts/termux-sync.sh'
